"""
RETIRED — superseded by src/declarative/memory_repository.py (GT-03).

This file is kept as a tombstone so that any lingering import will fail
loudly rather than silently importing a stale implementation.

Do not add new code here. Use src.declarative.MemoryRepository instead.
"""

raise ImportError(
    "src.declaritive.db_manager is retired. Use src.declarative.MemoryRepository (GT-03) instead."
)
